# Velair Climate theme

Home Assistant theme (light + dark modes) transcribed from the Sigma Design System tokens
("figma" token layer): Inter type scale, purple-600 accent, gray-900 text, 12 px cards with an
inset ring and soft shadow, 4 px spacing. Install through HACS (category: Theme), then select
"Velair Climate" in your profile or per dashboard.
